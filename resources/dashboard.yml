resources:
  dashboards:
    executive_dashboard:
      display_name: "Ride Analytics — Executive Command Center"
      file_path: ./Ride Analytics - Executing Command Center.lvdash.json
      warehouse_id: ${var.warehouse_id}
      embed_credentials: false